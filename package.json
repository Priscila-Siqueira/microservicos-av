{
  "name": "microservicos-av",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "product": "npm run dev -w @projeto/product-service",
    "order": "npm run dev -w @projeto/order-service",
    "gateway": "npm run dev --workspace=apps/api-gateway"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}